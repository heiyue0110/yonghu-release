# 永狐 1.4.16 更新公告

发布日期：2026-07-26

1.4.16 集中修复预警链路中的漏报、假启动和状态残留。屏幕检测不再要求目标闪烁，静止红色标记连续命中即可触发；频道、屏幕和 KM 的启停状态改为以后端真实运行结果为准。

## 屏幕预警

- 目标颜色连续两帧满足亮度、像素数和连通区域条件即可预警，静止目标不再漏报。
- 单帧噪点、清空帧、偏蓝偏白和过暗红色仍会过滤，避免扩大误报。
- 预警页新增“检测当前画面”，直接走正式截图与检测器，显示命中像素、比例和连续帧结果，不会注入测试预警。
- 目标清除后再次进入会作为独立事件提醒，不再被前端每分钟三条的二次限制截断；写入暂时失败时会继续重试。
- 截图、探测与缓存回退都重新检查模块启用状态和有效选区，关闭后不会继续取图。
- 屏幕模块继续保持风险确认后主动启用，默认不会替用户开启。

## 监控状态与配置

- 启动全部后逐项等待频道、屏幕和 KM 的真实状态；缺少哪项会明确提示，不再先显示假成功。
- 后端已运行但单个监控缺失时会自动补启；重复启动不会重复写入“已启动”预警。
- 启动、停止和配置命令改为独立队列，连续操作不会互相覆盖；后端启停串行执行，失去心跳后可重新拉起。
- 屏幕、频道与其他局部配置在跨进程锁内原子合并，避免同时写入时互相覆盖。
- 明确停止会持久化关闭自动恢复意图，下次打开不会被旧状态重新拉起。

## 频道、KM 与通知

- 更换 ChatLogs 目录或频道后会清除旧运行状态，不再把失效频道显示为仍在扫描。
- 路线频道命中会执行对应规则；无法解析星系或没有生产/路线范围的频道消息保留声音与 Windows 通知回退。
- 页面隐藏时由应用外壳统一通知，实时事件与轮询使用同一去重键；声音队列逐条保留频道、KM 和本地预警类别。
- 已解析且范围外的频道消息继续静默降噪；清空历史不再覆盖同时到达的新预警。
- KM 的 victim 联盟过滤、跨重启去重、独立通知开关和稳定事件键继续保留。

## 更新说明

- 可直接覆盖安装，现有配置和缓存会继续保留。
- 软件更新仍需用户确认；取消后不会下载、安装或重启。
- 安装器与自动更新使用配套 Tauri 签名，签名校验失败会直接终止。

安装包：EverFox_1.4.16_x64_setup.exe

SHA256：582E2D4CEA7AF1476DE4E5205EA023891223F3C40A63A274491F21AD3B24559C
